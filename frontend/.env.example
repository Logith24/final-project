# Optional: if you later call your Render API from the React app
# REACT_APP_API_URL=https://your-service-name.onrender.com

# CRA reads this at build time (GitHub Actions workflow sets homepage in package.json for Pages)
